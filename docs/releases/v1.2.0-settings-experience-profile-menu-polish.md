# TransitOps Platform v1.2.0 - Settings Experience & Profile Menu Polish

## Release objective

Improve the Settings experience and user profile menu interactions in TransitOps Platform.

This release focuses on making Settings clearer, more modular and more professional by separating account profile management from language preferences and polishing the authenticated user dropdown interactions.

## Scope

- Redesign `/settings` as a simple Settings overview.
- Replace the current left account summary card with cleaner navigation cards.
- Add a dedicated `/settings/profile` section for account profile management.
- Add a dedicated `/settings/language` section for language preferences.
- Redesign the language preferences panel using the Figma Make reference as visual guidance.
- Polish the navbar user profile dropdown hover, focus and active states.
- Keep logout visually distinct from regular navigation actions.
- Preserve existing authentication, role access, session synchronization and i18n behavior.

## Out of scope

- Back-End changes.
- Prisma schema changes.
- New API endpoints.
- Authentication logic changes.
- New third-party UI or animation libraries.
- Full E2E test setup.
- Deployment configuration.

## Technical approach

- Keep Settings behind the existing authenticated route protection.
- Split the Settings feature into smaller components.
- Reuse `AuthService` for profile, password and avatar actions.
- Reuse `LanguageService` for language selection and persistence.
- Use Angular templates and SCSS transitions instead of external animation libraries.
- Preserve English and Spanish translations.

## Proposed routes

```txt
/settings
/settings/profile
/settings/language
```

## Validation checklist

- [x] `npm run build`
- [x] `npm run test:api`
- [x] `npm run api:real:typecheck`
- [x] `npm run api:real:test`
- [x] `npm run api:real:build`
- [x] `/settings` renders the new overview screen.
- [x] `/settings/profile` renders account profile management.
- [x] `/settings/language` renders the redesigned language panel.
- [x] Language switching works immediately.
- [x] Language preference persists after refresh.
- [x] Avatar upload and delete still work.
- [x] Profile update still works.
- [x] Password change keeps the expected security behavior.
- [x] Navbar profile dropdown hover, focus and active states are visible.
- [x] Logout action has a distinct visual treatment.
- [x] Light theme remains readable.
- [x] Dark theme remains readable.
- [x] Responsive layout works at mobile, tablet and desktop sizes.

## Final QA results

### Automated validation

The following commands were executed successfully:

```bash
npm run build
npm run test:api
npm run api:real:typecheck
npm run api:real:test
npm run api:real:build
```

### Manual validation

- `/settings` renders the redesigned overview screen.
- `/settings` shows Profile and Language as separated configuration sections.
- The previous combined Settings layout and legacy left account card were removed.
- `/settings/profile` renders profile, avatar and password management.
- Avatar upload and delete behavior remains functional.
- Profile update behavior remains functional.
- Password update behavior remains functional.
- `/settings/language` renders the redesigned language preferences panel.
- Language selection updates the interface immediately.
- Language preference persists after refresh.
- Language dropdown closes with Escape.
- Language dropdown closes when clicking outside.
- Language dropdown stays visually above the lower information note.
- Navbar profile dropdown opens and closes correctly.
- Navbar Profile action navigates to `/settings/profile`.
- Navbar Settings action navigates to `/settings`.
- Logout keeps a distinct danger visual treatment.
- Hover, focus and active states are visible in the navbar dropdown.
- Light theme remains readable.
- Dark theme remains readable.
- Responsive layout was checked at mobile, tablet and desktop sizes.

### QA fix included

A visual stacking issue was found in the language preferences dropdown where the lower information note appeared above the opened menu. The issue was resolved by adjusting the dropdown layering so the menu stays above the note.

## Release notes

### Added

- Dedicated Settings overview screen.
- Dedicated Profile settings route at `/settings/profile`.
- Dedicated Language settings route at `/settings/language`.
- Redesigned language preferences panel inspired by the Figma Make reference.
- Improved navbar profile dropdown interactions.

### Changed

- Settings is now split into focused sections instead of one combined screen.
- Navbar Profile now routes directly to `/settings/profile`.
- Navbar Settings keeps routing to `/settings`.
- Profile, language and navbar UI now use clearer hover, focus and active states.

### Removed

- Legacy combined Settings component.

### Fixed

- Language dropdown now remains visually above the lower information note when opened.
